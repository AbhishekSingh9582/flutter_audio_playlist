enum RepeatMode {
  off, // Normal playback, then move to next
  repeatOnce, // Repeat the current track one time, then switch to off and move to next
  repeatCurrent, // Repeat the current track indefinitely
}
